import { Component } from '@angular/core';

// this is needed to make the component work
@Component({
  selector: 'app-acomponent',      // the name used in HTML to show this component
  imports: [],                   
  templateUrl: './acomponent.html', // the HTML file for this component
  styleUrl: './acomponent.css',     // the CSS file for this component
})

export class AComponent {
  // put your code here

}